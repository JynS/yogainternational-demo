import React from "react";
import ReactDOM from "react-dom";
import ClassSearch from "./components/classSearch";

// This is where we're gonna actually "start" React on the client
// Will probably only need to import Filter, Class (?)

// the whole thing

// TODO: keep track of the results count as a state

ReactDOM.render(<ClassSearch />, document.getElementById("root"));